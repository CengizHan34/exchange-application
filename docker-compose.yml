version: '3.1'

services:
  exchange:
    image: exchange-application:0.0.1-SNAPSHOT
    restart: always
    container_name: exchange-application
    ports:
      - 8080:8080
    environment:
      EXCHANGE_SERVICE_PORT: 8080